
#compile
gcc -o test test.c mqtt_client.c mqtt_socket.c mqtt_packet.c

# run ECG anomaly server
./test

# MQTT broker is already installed
# see the link below
# https://proni.tistory.com/entry/%F0%9F%90%A7-Ubuntu-mosquitto-mosquitto-client-%EC%84%A4%EC%B9%98%ED%95%98%EA%B8%B0

# run ECG sensor client
mosquitto_pub -d -t wolfMQTT/test/ECG -m "10"

# repeat the above command 10 times (since the window size is 10)

# send abnormal ECG sensor data (abs(average - curr) > 30)
mosquitto_pub -d -t wolfMQTT/test/ECG -m "100"
